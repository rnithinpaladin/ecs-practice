name: resubale build workflow 

on:
  workflow_call:
    inputs: 
      node_version: 
        required: true
        type: string
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout/@v4
      - name: Testing phase
        run: |
          echo "i'm in"
        
        
